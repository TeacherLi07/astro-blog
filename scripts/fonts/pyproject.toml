[project]
name = "teacherli-font-tools"
version = "0.1.0"
description = "Pinned font subsetting tools for the Astro blog build"
requires-python = ">=3.12"
dependencies = [
  "fonttools[woff]==4.63.0",
]

[tool.uv]
package = false
